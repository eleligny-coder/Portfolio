import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { PageIntro, SectionTitle } from "@/components/layout";
import { services } from "@/data/site";
export function generateStaticParams(){return services.map(service=>({slug:service.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const service=services.find(item=>item.slug===slug);return service?{title:service.title,description:service.summary}:{title:"Service"}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=services.find(item=>item.slug===slug);if(!service)notFound();return <><PageIntro eyebrow="Service" title={service.title} text={service.summary} actions={<Link className="btn" href="/contact">Demander un échange</Link>}/><section className="section alt"><div className="shell grid two"><div><SectionTitle eyebrow="Pour qui" title="Publics concernés"/><ul className="list">{service.audience.map(item=><li key={item}>{item}</li>)}</ul></div><div><SectionTitle eyebrow="Résultat" title="Ce que l’intervention doit produire"/><ul className="list">{service.outcomes.map(item=><li key={item}>{item}</li>)}</ul></div></div></section><section className="section"><div className="shell"><SectionTitle eyebrow="Livrables" title="Des éléments directement exploitables"/><div className="grid three">{service.deliverables.map((item,index)=><article className="card" key={item}><span className="eyebrow">{String(index+1).padStart(2,"0")}</span><h3>{item}</h3></article>)}</div></div></section><section className="section alt"><div className="shell"><SectionTitle eyebrow="Déroulé" title="Une méthode courte et traçable"/><div className="workflow">{service.process.map((item,index)=><div key={item}><span>{String(index+1).padStart(2,"0")}</span><strong>{item}</strong></div>)}</div></div></section><section className="section"><div className="shell cta"><div><span className="eyebrow">Cadrage</span><h2>Décrire le besoin avant de chiffrer la solution.</h2></div><Link className="btn" href="/contact">Présenter votre contexte</Link></div></section></>}
