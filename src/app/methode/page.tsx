import type {Metadata} from "next";import {PageIntro} from "@/components/layout";import {method} from "@/data/site";export const metadata:Metadata={title:"Méthode"};export default function Page(){return <><PageIntro eyebrow="Méthode" title="Une exécution structurée, sans bruit." text="Chaque étape réduit une incertitude : besoin, périmètre, architecture, expérience, livraison, qualité puis adoption."/><section className="section"><div className="shell">{method.map(([n,t,d])=><div className="step" key={n}><b>{n}</b><div><h2>{t}</h2><p>{d}</p></div></div>)}</div></section></>}
