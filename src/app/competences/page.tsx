import type {Metadata} from "next";import {PageIntro} from "@/components/layout";import {skills} from "@/data/site";export const metadata:Metadata={title:"Compétences"};export default function Page(){return <><PageIntro eyebrow="Compétences" title="Des capacités organisées autour de la livraison." text="Pas une collection de logos : un ensemble cohérent pour relier produit, code, données, paiement, IA et pilotage."/><section className="section"><div className="shell grid two">{skills.map(([t,d],i)=><article className="card" key={t}><span className="eyebrow">0{i+1}</span><h2>{t}</h2><p>{d}</p></article>)}</div></section></>}
