import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { PageIntro, SectionTitle } from "@/components/layout";
import { getTraining, trainings } from "@/data/trainings";
export function generateStaticParams(){return trainings.map(training=>({slug:training.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const training=getTraining(slug);return training?{title:training.title,description:training.summary}:{title:"Formation"}}
export default async function TrainingPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const training=getTraining(slug);if(!training)notFound();return <><PageIntro eyebrow={training.category} title={training.title} text={training.summary} actions={<Link className="btn" href="/contact">Demander cette formation</Link>}/><section className="section alt"><div className="shell detail-grid"><div><SectionTitle eyebrow="Public" title="À qui s’adresse la formation ?"/><ul className="list">{training.audience.map(item=><li key={item}>{item}</li>)}</ul></div><article className="card"><span className="eyebrow">Prérequis</span><h2>Conditions d’entrée</h2><p>{training.prerequisites}</p><h3>Formats disponibles</h3><div className="tags">{training.formats.map(item=><span key={item}>{item}</span>)}</div></article></div></section><section className="section"><div className="shell grid two"><div><SectionTitle eyebrow="Objectifs" title="Compétences visées"/><ul className="list">{training.objectives.map(item=><li key={item}>{item}</li>)}</ul></div><div><SectionTitle eyebrow="Programme" title="Déroulé principal"/><ol className="number-list">{training.program.map(item=><li key={item}>{item}</li>)}</ol></div></div></section><section className="section alt"><div className="shell grid two"><div><SectionTitle eyebrow="Pratique" title="Exercices et cas"/><ul className="list">{training.exercises.map(item=><li key={item}>{item}</li>)}</ul></div><div><SectionTitle eyebrow="Supports" title="Livrables remis"/><ul className="list">{training.deliverables.map(item=><li key={item}>{item}</li>)}</ul></div></div></section></>}
