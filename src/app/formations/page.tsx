import type { Metadata } from "next";
import Link from "next/link";
import { PageIntro, SectionTitle } from "@/components/layout";
import { trainings } from "@/data/trainings";
export const metadata: Metadata = { title: "Formations", description: "Formations premium en entrepreneuriat, IA, SaaS, automatisation, WordPress et ESS." };
export default function TrainingsPage(){const categories=[...new Set(trainings.map(item=>item.category))];return <><PageIntro eyebrow="Formation" title="Des programmes construits à partir de projets réels." text="Les formations combinent méthode, exercices, modèles et retours d’expérience issus de SaaS, marketplaces, outils métiers et automatisations réellement conçus."/><section className="section"><div className="shell">{categories.map(category=><div className="training-category" key={category}><SectionTitle eyebrow="Catalogue" title={category}/><div className="grid two">{trainings.filter(item=>item.category===category).map(training=><article className="card" key={training.slug}><span className="eyebrow">{training.category}</span><h3>{training.title}</h3><p>{training.summary}</p><div className="tags">{training.formats.slice(0,3).map(format=><span key={format}>{format}</span>)}</div><Link className="text-link" href={`/formations/${training.slug}`}>Voir le programme →</Link></article>)}</div></div>)}</div></section><section className="section alt"><div className="shell cta"><div><span className="eyebrow">Organismes & entreprises</span><h2>Adapter un programme à votre public.</h2><p>Formats de 2 heures à 5 jours, en atelier ou accompagnement personnalisé.</p></div><Link className="btn" href="/contact">Demander un programme</Link></div></section></>}
